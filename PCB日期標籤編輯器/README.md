# PCB 日期標籤編輯器

此版本新增：

- 預設加入「細明體（MingLiU）」
- 啟動時自動檢查並隱藏無法使用的字體
- 手動加入字體前先驗證
- 讀取本機字體後只保留能正常顯示的字體
- 本機字體無法嵌入 SVG 時，仍允許 PNG 正常輸出

## GitHub Pages

將本資料夾內容上傳到 Repository 根目錄，再到：

`Settings → Pages → Deploy from a branch → main → /(root)`

即可發布。
